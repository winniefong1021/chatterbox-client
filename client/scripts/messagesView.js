var MessagesView = {

  $chats: $('#chats'),

  initialize: function() {
    // append data (username, text, roomname) received from app to $chats
    // use messageView.renderrr() template
    // console.log(data.results);

    //   for (var i = 0; i < data.results.length; i++) {
    //     console.log(data.results[i].text);
    //   }
    // MessagesView.$chats.on()
  },

  render: function(data) {
    console.log(data);
  }

};